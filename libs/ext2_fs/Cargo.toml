[package]
name = "ext2_fs"
version = "0.0.1"
edition = "2021"

[lib]
name = "ext2_fs"
path = "src/lib.rs"

[features]
no_std = []

[dependencies]
spin = "0.7.0"
lazy_static = { version = "1.4.0", features = ["spin_no_std"] }
